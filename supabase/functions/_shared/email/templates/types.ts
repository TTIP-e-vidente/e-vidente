/** Generado por sync-email-templates-edge.cjs */
export interface WelcomeTemplateContext {
  name: string;
  mail: string;
  playUrl?: string;
  leaderboardUrl?: string;
}

export interface StreakTemplateContext {
  name: string;
  mail: string;
  streakCount: number;
  playUrl?: string;
  leaderboardUrl?: string;
}
